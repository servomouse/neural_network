# example comment

inputs: [
    0,      # single index
    1:31    # range
]

groups: [
    8 x [inputs[0:15] except[idx]] type linear,
    8 x [inputs[15:31]] type linear,
    4 x [group[0], group[1]] type linear
]

outputs: [
    4 x [group[2]] type poly
]
